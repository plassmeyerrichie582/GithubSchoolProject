def greet(name):
    """
    Simple function to greet someone by their name.
    
    Parameters:
    name (str): The name of the person to greet.
    
    Returns:
    str: A greeting message.
    """
    return f"Hello, {name}!"

# Example usage and output
print(greet("Alice"))
